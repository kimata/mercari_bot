services:
    mercari-bot:
        build:
            context: .
            dockerfile: Dockerfile
        tty: true
        stdin_open: true
        volumes:
            - ./data:/opt/mercari-bot/data
